namespace Excepciones
{
    class LanzarExcepciones
    {
        public LanzarExcepciones()
        {
            throw new Exception("Esta es una excepcion lanzada por el programador");
        }
    }
}
